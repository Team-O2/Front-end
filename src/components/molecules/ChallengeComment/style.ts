import Styled from 'styled-components';

const SCommentList = Styled.div`
display : flex;
flex-direction : column;
align-items : flex-end;
margin : 0 auto;
width : 724px;
  .comment__write{
    margin-bottom:30px;
  }
`;

export default SCommentList;
